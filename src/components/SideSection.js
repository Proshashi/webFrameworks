import React from "react";

export default function SideSection() {
  return (
    <div
      style={{
        background: "violet",
        color: "white",
        padding: "10px",
      }}
    >
      Side News
    </div>
  );
}
